@using Ztop.Todo.ActiveDirectory
@model List<Ztop.Todo.Model.ADGroup>

    
@{ 
    var organications = Model.Where(e => e.Type == Ztop.Todo.Model.ADType.Organication).ToList();
    var big = XmlHelper.GetDitrict();
}

<script>
    $(function () {
        $("#Manager").AutoComplete({
            'data': "/Admin/Gain",
            'width': 'auto',
            'itemHeight': 50,
            'ajaxDataType': 'json',
            'onerror': function (msg) { alert(msg);}
        })
    })
</script>



<form class="form-horizontal" method="post" action="">
    <div class="row">
        <div class="col-md-12">
            <div class="form-group">
                <label for="" class="control-label col-md-2">管理者：</label>
                <div class="col-md-9">
                    <input type="text" name="Manager" class="form-control" id="Manager" autocomplete="off" />
                </div>
            </div>
            <div class="form-group">
                <label for="" class="control-label col-md-2">行政区：</label>
                <div class="col-md-9" id="City">
                    <div class="col-md-2">
                        <ul class="nav nav-pills nav-stacked" role="tablist">
                            @foreach (var item in big)
                            {
                                <li role="presentation"><a href="#@(item)" role="tab" data-toggle="tab">@(item)</a></li>
                            }
                        </ul>
                        <hr />
                        <button type="submit" class="btn btn-primary"><i class="glyphicon glyphicon-ok"></i>确定</button>
                    </div>
                    <div class="col-md-10">
                        <div class="tab-content">     
                            @foreach(var item in big)
                            {
                                var code = item.Length > 4 ? item.Substring(0, 4) : "";
                                var groups = organications.Where(e => e.Code == code);
                                <div role="tabpanel" class="tab-pane" id="@(item)">
                                    <div class="col-md-2">
                                        <ul class="nav nav-pills nav-stacked" role="tablist">
                                            @foreach (var first in groups)
                                            {
                                                <li role="presentation"><a href="#first-@(first.Name)" role="tab" data-toggle="tab">@(first.Name)</a></li>
                                            }
                                        </ul>
                                    </div>
                                    <div class="col-md-10">
                                        <div class="tab-content">
                                            @foreach(var first in groups)
                                            {
                                                <div role="tabpanel" class="tab-pane" id="first-@(first.Name)">
                                                    <table class="table">
                                                        @foreach(var entry in Model.Where(e => e.OID == first.ID).ToList().ListToTable())
                                                        {
                                                            <tr>
                                                                @foreach(var second in entry)
                                                                {
                                                                    if (second != null)
                                                                    {
                                                                        <td>
                                                                            <label class="checkbox-inline">
                                                                                <input type="checkbox" value="@(second.Name)" name="GroupName" /><strong>@(second.Name)</strong>
                                                                            </label>
                                                                        </td>
                                                                    }
                                                                }
                                                            </tr>
                                                        }
                                                    </table>
                                                </div>
                                            }
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                    @*<div class="col-md-9">
                        <div class="tab-content">
                            @foreach (var item in big)
                            {
                                var code = item.Length > 4 ? item.Substring(0, 4) : "";
                                <div role="tabpanel" class="tab-pane" id="@(item)">
                                    <div class="panel-group" id="acc-@(item)" role="tablist" aria-multiselectable="true">
                                        @foreach (var first in organications.Where(e => e.Code == code))
                                        {
                                            <div class="panel panel-default">
                                                <div class="panel-heading" role="tab" id="@(first.Name)">
                                                    <h4 class="panel-title">
                                                        <a data-toggle="collapse" data-parent="#acc-@(item)" href="#collapse@(first.Name)" aria-expanded="true" aria-controls="collapse@(first.Name)">
                                                            @(first.Name)
                                                        </a>
                                                    </h4>
                                                </div>
                                                <div id="collapse@(first.Name)" class="panel-collapse collapse" role="tabpanel" aria-labelledby="@(first.Name)">
                                                    <div class="panel-body">
                                                        <table class="table">
                                                            @foreach (var entry in Model.Where(e => e.OID == first.ID).ToList().ListToTable(2))
                                                            {
                                                                <tr>
                                                                    @foreach (var second in entry)
                                                                    {
                                                                        if (second != null)
                                                                        {
                                                                            <td>
                                                                                <label class="checkbox-inline">
                                                                                    <input type="checkbox" value="@(second.Name)" name="GroupName" /><strong>@(second.Name)</strong>
                                                                                </label>

                                                                            </td>
                                                                        }
                                                                    }
                                                                </tr>
                                                            }
                                                        </table>
                                                    </div>
                                                </div>
                                            </div>
                                        }
                                    </div>

                                </div>

                            }
                        </div>
                    </div>*@
                </div>
            </div>
            <div class="col-md-6 col-md-offset-2">
               
            </div>
        </div>
    </div>

    

</form>





